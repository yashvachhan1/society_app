name: society_core
description: "Shared design system, theme, widgets and utilities for the Society monorepo (used by both the resident mobile app and the admin web dashboard)."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.12.1
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Shared UI typography
  google_fonts: ^6.2.1

  # Formatting (currency, dates) used across apps
  intl: ^0.20.2

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
