# widgets.dart (barrel)

Mirrors `lib/core/widgets/widgets.dart`.

**Purpose** — one import that exposes every shared widget.

```dart
import 'package:society_app/core/widgets/widgets.dart';
```

## Exports

`app_card.dart` · `detail_row.dart` · `empty_state.dart` · `icon_chip.dart` ·
`info_banner.dart` · `labelled_field.dart` · `section_header.dart` ·
`skyline_logo.dart` · `status_chip.dart`

## Notes

When a new shared widget is added to `lib/core/widgets/`, export it here too.
