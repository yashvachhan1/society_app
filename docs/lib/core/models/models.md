# models.dart (barrel)

Mirrors `lib/core/models/models.dart`.

**Purpose** — one import for every domain model.

```dart
import 'package:society_app/core/models/models.dart';
```

## Exports

`app_user.dart` · `membership.dart` · `society.dart` · `unit.dart`

## Notes

Each model mirrors a table in the Society Management Platform database schema
(v1.0): `users`, `memberships`, `societies`, `towers` + `units`.
